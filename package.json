{
  "name": "redwave_overlays",
  "version": "1.0.0-dev",
  "main": "frontend/main.js",
  "scripts": {
    "start:dev": "cross-env NODE_ENV=development concurrently \"uvicorn backend.main:app --reload --port 8000\" \"electron .\"",
    "start:prod": "cross-env NODE_ENV=production concurrently \"uvicorn backend.main:app --port 8000\" \"electron .\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.0.0",
    "electron": "^28.1.0"
  },
  "author": "onesch",
  "license": "GPL-3.0",
  "description": ""
}
